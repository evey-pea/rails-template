<h1><%= @search_heading %></h1>

<%= form_with(url: search_nearby_path, method: "get") do %>
  <%= label_tag(:q, "Search for profiles within km:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>
<%= render 'search_results' %>
